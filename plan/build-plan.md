# Build plan

2026-09-22. Sprint-by-sprint, task-level. Built from `architecture/02-technical-architecture-v2.md`, `product/05-product-spec-v2.md`, `plan/market-order.md`, and `plan/pre-build-readiness.md`. Twelve weeks in six sprints; each sprint has a definition of done that a non-technical founder can verify. The co-founder builds; the founder runs accounts, families, and reviews.

Conventions: every task names its spec section; "DoD" is the definition of done; tasks marked **F** are the founder's; tasks marked **Built** have their code and tests in the repository, green, while their DoD has still to be shown; a sprint is not done until its DoD is demonstrated on a real device or a real family.

## Sprint 0 · Day 0 (one afternoon, before sprint 1)

| # | Task | Owner | DoD |
|---|---|---|---|
| 0.1 | Monorepo bootstrap: pnpm + Turborepo, Biome, TypeScript strict, `apps/worker` (Hono), `packages/contracts`, `packages/core`, `packages/db` (Drizzle), CI skeleton (typecheck, lint, unit) | co-founder | `pnpm test` green on an empty suite; CI runs on PR |
| 0.2 | Accounts: Cloudflare ("Vela" and "Vela staging", each with its workers.dev subdomain, `vela-light` and `vela-light-staging`; no domain is bought, ADR-26), Neon (two projects in Singapore, `vela-staging` and `vela`, created 17 September 2026), Anthropic key, Deepgram key, Sentry, GitHub email for failed workflows (the watchdog), Expo/EAS, Telegram bot (BotFather), LINE Official Account + Messaging API channel (unverified, founder's name); GitHub environments `staging` and `production` (production with the founder as required reviewer) | **F** | Keys pasted by the founder into each Worker's secrets (the pilot Worker `vela` and the admin Worker `vela-admin`), and `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, and `DATABASE_URL` into each GitHub environment's secrets (no repository secrets); both Workers deploy to staging |
| 0.3 | Schema applied with `packages/db/migrations/0000_init.sql` to the database `neondb` of the Neon project `vela-staging`, then of the project `vela`, each on its default branch (one project per environment, so staging never copies production data; `infra/README.md`, section 2); PGlite tests in CI. Until the first database is migrated, a schema change regenerates `0000_init.sql` instead of adding `0001`. Migrations run in the deploy job before `wrangler deploy` (1.15); until an environment holds its secrets, the founder runs `pnpm --filter @vela/db migrate` from their own terminal (`infra/runbooks/release.md`) | co-founder; the migration run **F** | 34 tables in both projects; the one-per-day and actor tests pass in CI |
| 0.4 | `docs/` runbooks: incident (one page), sub-processor list, OTA policy, restore drill | co-founder | Files exist and are linked from the README |

## Sprint 1 · Weeks 1–2 · The instrument, on the real platform

Goal: the phase-0 loop (spec Appendix A) running on Telegram, first in a dogfooding week on staging and then for three to five families living in Taiwan, on the production architecture (Worker, Neon, DO scheduler, gateway, events), so nothing is thrown away.

| # | Task | Spec | DoD |
|---|---|---|---|
| 1.1 | Privacy notice and consent script, EN and ZH-TW; `consents` rows written with text version | §9, §17 | Used in the dogfooding week (1.10), then read on each first family's onboarding call; no objection; stored with version |
| 1.2 | **Built** `packages/core`: exchange state machine, composer (priority rules, read-back opening, fallback hello), ladder timings, budget kinds, local-day and DST math; unit tests incl. DST cases | §3, §4, §8 | 100% coverage on core; DST suite green |
| 1.3 | **Built** Member Durable Object: alarms for arrival, repeat, quiet, turn prompt; recompute from zone; storage cleared when the member stops, leaves, dies, or is deleted; reconciliation cron; heartbeat recorded in `ReconcileHeartbeat` | arch §6 | Silence drill part 2 passes; `/healthz` answers ok and a watchdog run is green (`infra/README.md` section 6) |
| 1.4 | **Built** Outbound gateway with the `outbound` table, budget index, actor CHECK, retries, DLQ; a send refused because the group became a supergroup re-points the family group and is sent again at once without counting an attempt | arch §7 | A second arrival for the same day is rejected in an integration test; a send to an upgraded group reaches the new id |
| 1.5 | **Built** Telegram adapter: send with inline buttons and voice; webhook verify + parse (message, callback, voice, reaction, blocked); fixtures | arch §8 | Contract tests pass, tampered fixture fails |
| 1.6 | **Built** Family Telegram group flow: 19:00 prompt, "reply to ask", `whenever:` items, "Into her morning" confirmation; her private-chat arrival with buttons; answer capture (light lit synchronously), counting a message sent before the arrival as that day's answer; her messages ignored until she taps yes and after a no; read-back next morning | App. A | In the dogfooding week on staging (1.10), the founder as organiser and the kept-light member complete one full loop: ask → answer → replies → read back; a message typed before the consent tap reaches nobody |
| 1.7 | **Built** AI service v0: `understand`, `flag`, `chips`, `readback`, `hello` with Zod schemas, structured outputs, prompt registry, `ai_calls` logging; Deepgram STT; Promptfoo golden set v0 (50 synthetic cases) gating in CI. Not yet in CI: the golden set (79 cases) and its flag-recall gate run by hand with an API key (`pnpm --filter @vela/ai eval`) | arch §9 | Evals green; a flagged synthetic answer produces an organiser notice |
| 1.8 | **Built** Quiet notice to the organiser in Telegram (facts, nearby contacts who said yes as text, buttons "she's fine", "wait 2 h"); learning period logic | §8 | A simulated silent day produces one notice at T_quiet, none on delivery failure; a contact without a recorded yes never appears |
| 1.9 | **Built** Event log + `metrics_daily` job + a first admin page behind Cloudflare Access, served by its own Worker `vela-admin` (1.16): an overview of every family's day without content (deliveries, answer and quiet states and times, AI call failures), and a page per family with its answers, summaries, and AI outputs; every view writes `admin_access_log`, one row for each family the page shows; flag and draft messages to the founder's chat carry a link, never her words | §18, arch §15 | Founder can see every family's day on one page; opening the overview logs a `view` row for each family listed |
| 1.10 | **F** Dogfooding week on staging, starting by day 7 (decided 18 September 2026): the founder is the organiser, and a second Telegram account of the founder's, or a friend living in Taiwan who agrees, is the kept-light member; a friend takes part only once the C9 data processing tasks are done and uses only scripted test content, so staging holds only synthetic data and a friend's name, Telegram account and consent rows, deleted when the week ends (status below, item 5). The week exercises onboarding, consent, the health-words question, arrivals, answers, the quiet ladder, the weekly read, stop and start, the admin page, and the watchdog. AI is off on staging for the week (1.17; decision X, 18 September 2026), so it has no summaries, flag checks, translations, chips, or AI-written weekly read lines. Media storage is off too (1.18; decision M, 20 September 2026), so Vela keeps no copy of the week's voice notes and photos: Telegram holds them, and the loop, a voice answer's transcript included, runs unchanged. Then, only after the week passes and production is deployed, three to five families living in Taiwan (the founder's friends and their families) from day 14, each within "Who can join" in the pilot README; onboarding calls double as interviews; UCLA-3 at week 0. The founder's own parent is not onboarded (status below, item 5) | App. A | Every part of the dogfooding week has run at least once on staging, including a quiet notice, a stop and a start, a sent weekly read, and a green watchdog run. While AI is off, the week does not require the AI steps: a summary, a flag notice, a translation, chips, or an AI-written draft (1.7, 1.13 and 1.14 show those only once AI is on, and switching staging on needs the founder's approval of buying Anthropic credit; `infra/README.md`, section 3). Three to five families living in Taiwan live on production |
| 1.11 | **Built** Departures: Telegram's `left_chat_member` becomes `member_left`; in a linked group a member who leaves (not an organiser, not the kept-light member) becomes `left` and drops out of the turn rotation; an organiser or the kept-light member leaving changes nothing and sends the founder `admin.member_left_group`; someone with no link stores nothing; a member who left and asks, replies, or reacts in the group again is active again | §2, §7 | In the staging group, a sibling who leaves is not named in the next turn prompt; an organiser who leaves keeps their state and the founder gets one note |
| 1.12 | **Built** Admin write actions on the Access-protected page (the admin Worker `vela-admin`, 1.16): record a pilot or privacy-notice consent; record a nearby contact's yes or no; add or remove a contact; set or end an away period; mark a member left; mark the kept-light member deceased (light off, schedule cleared, nothing sent, messages already queued dropped, group asks ignored); request family deletion; each a POST form with the Access token and same-origin checks, writing `admin_access_log` and an event | §9, §17, arch §11 | The founder records pilot, privacy-notice, and nearby-contact consents from the page instead of the Neon console; each action appears in `admin_access_log` with its event; a request without a valid Access token or from another origin is refused |
| 1.13 | **Built** Understanding re-run: `processing_attempts` counted per answer; `understood_at` only when understand and flag both succeed; `reconcile` re-enqueues answers not understood after 15 minutes and within 24 hours, up to 3 attempts; `admin.understand_failed` once after the third | §5, arch §9 | A voice answer whose transcription fails twice is transcribed on its third ingestion and understood, with one transcript post and one flag notice; a text answer whose flag check fails twice is understood on its third attempt with one flag notice; an answer that ends its third attempt not understood sends the founder one note |
| 1.14 | **Built** Weekly read draft and send: the Sunday draft stores the week's counts in `weekly_reads.stats` and gives the AI only the days the parent answered; the founder edits the draft's lines and suggestion on the admin page, sees the count lines but cannot edit them, and taps Send; the sent lines and suggestion are stored, and each organiser gets in Telegram the counts rendered from numbers (`renderWeeklyRead`), then the lines, then the suggestion; a Send that reaches no organiser is refused rather than recorded as sent; "what does the family see" shows the latest sent read's lines after the summaries, never its counts or suggestion, translated into the parent's language when the family's differs | §8, §9, §13, App. A | In the dogfooding week (1.10), the founder as organiser receives the first weekly read from the Send button, opening with the counts; the kept-light member's "what does the family see" includes its lines, in that member's language, with no count and no suggestion |
| 1.15 | **Built** GitHub deploy workflow: environments `staging` (merge to `main`) and `production` (tag, founder's approval); `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, and `DATABASE_URL` as environment secrets only; the checks, the migrations once, then the pilot Worker `vela`, then the admin Worker `vela-admin` | arch §16–17 | A merge to `main` migrates and deploys both staging Workers with no hand step; a tag waits for the founder's approval, then migrates and deploys both production Workers; the repository has no secrets |
| 1.16 | **Built** Hosting on workers.dev with no domain (ADR-26): the admin pages move to their own Worker `vela-admin`, protected as a whole by Cloudflare Access, which the Worker still checks; the pilot Worker `vela` serves the webhook, `/healthz`, and the privacy notices at `/privacy` and `/privacy/zh-TW`, rendered from `plan/materials/pilot` into a committed module that CI holds to the Markdown, and refuses to start outside development while a notice holds a bracketed blank; `ADMIN_CONVERSATION_ID` becomes a secret; the Telegram setup script registers the pilot origin and keeps pending updates unless `--drop-pending-updates` is given | §9, §17, arch §17 | On staging, both notice pages open on a phone with no sign-in; every address of `vela-admin` asks for the Access sign-in, and `/admin` on `vela` answers not found; `getWebhookInfo` shows the pilot origin with no error |
| 1.17 | **Built** AI off (decision X, 18 September 2026): the var `AI_PROVIDER`, `anthropic` or `off`, the same on both Workers of an environment (`off` in development and staging, `anthropic` in production); with `off` no AI provider is called and every AI step takes its safe default without counting a failure (no `ai_calls` row; an answer that reaches understanding is understood on that run, so no understanding re-run and no note to the founder follow, while a failed transcription is re-run as before), and each Worker logs `ai_off` once per start; the Anthropic key is needed only with `anthropic`; production refuses `off`; the setup script skips the Anthropic key with `off` and says how to switch AI on | arch §9 | On staging with AI off, the dogfooding week's loop runs with no Anthropic key, and each Worker's log shows `ai_off` |
| 1.18 | **Built** Media storage off (decision M, 20 September 2026): the pilot Worker's var `MEDIA_STORAGE`, `r2` or `off` (`off` in development and staging, `r2` in production, which refuses `off` because the privacy notice promises families that voice notes and photos are kept in Vela's own storage for 30 days and then deleted); an environment that is `off` binds no R2 bucket at all and Vela keeps no copy: the light, the family group post and a voice answer's transcript are unchanged, because Telegram carries the file and the pipeline fetches it from there, each `media` row keeps only what Telegram said about the file, its id, its type and its size, with no storage key, and retention deletes the row and writes its proof with no object to delete; the pilot Worker logs `media_storage_off` once per start; the setup script creates no bucket while storage is off, refuses production with `off` before it creates anything, and prints how to switch it on | arch §9, §17 | On staging with media storage off, a voice answer from the kept-light member lights the light, reaches the family group and is transcribed while nothing is written to R2, and the pilot Worker's log shows `media_storage_off`; production is deployed with `r2` and its bucket |

DoD for the sprint: the dogfooding week passed on staging, then three to five families living in Taiwan on the instrument in production for at least a week; answer rate and latency visible in the admin; no double send, no false quiet notice, consent recorded for every parent.

Status on 21 September 2026: the code tasks marked **Built** are in the repository with their tests green. Both staging Workers are deployed: `/healthz` answers `ok`, both privacy pages answer 200, the pilot Worker's `/admin` answers 404, and the founder has confirmed the admin overview opens after Cloudflare Access sign-in and the Telegram bot responds. Staging's identifiers and watchdog enablement are committed on `build/sprint-0-1`; the watchdog still needs its configuration on `main` and a verified green run. The founder has also confirmed the first scheduled arrival was answered and its answer reached the linked family group, and confirmed the friend-test participation and provider data-processing prerequisites. The dogfooding week is in progress; next-morning reply read-back, the remaining scenarios and the live silence drill are not yet verified, so the week has not passed. Production is not deployed. They include sprint 2's retention job (2.8), built ahead of its sprint because the privacy notice's 30-day promise depends on it. 1.1's documents are drafts in `plan/materials/pilot/`, and the code records consents with their text version. The pilot is free for families (decided 17 September 2026): no fee is taken and no payment data is collected, and willingness to pay is asked on each organiser's day-30 call (6.2). Hosting was decided on 17 September 2026 (ADR-26): no domain is bought; each Cloudflare account's free workers.dev subdomain carries two Workers, the pilot Worker `vela` and the admin Worker `vela-admin`, and Cloudflare Access protects the whole admin Worker instead of a path on a domain (1.16). AI is off in development and on staging (decided 18 September 2026, decision X; 1.17): Anthropic has no free tier, the founder has not approved buying credit, and the free alternatives were not taken (Google Gemini's free tier may use what it is sent to improve Google's products, which the privacy notice forbids; Groq would need new code and a change to the notice). Production cannot run with AI off. Media storage is off in development and on staging too (decided 20 September 2026, decision M; 1.18): Cloudflare R2 needs a subscription with a payment method even though the pilot's use stays inside its free monthly allowance, and the founder said R2 can wait, so staging binds no bucket and keeps no copy of a voice note or photo while Telegram holds them; Queues and Hyperdrive are on the Workers Free plan, so nothing else is blocked. Production cannot run with media storage off, because the privacy notice promises families that voice notes and photos are kept in Vela's own storage for 30 days and then deleted. What remains before the pilot runs:

1. **F** Accounts and choices (0.2): the accounts in `infra/README.md`, sections 1 to 10, including each Cloudflare account's workers.dev subdomain, `vela-light` for "Vela" and `vela-light-staging` for "Vela staging" (section 1, step 10), which fixes the hosts already written in the wrangler files; each bot's BotFather settings, with the privacy policy link to its environment's `/privacy` page (section 7, step 3); and each GitHub environment's three secrets (section 10). Nothing else is handed over: the founder pastes each key at the setup script's hidden prompts (items 3 and 4). The Anthropic account (`infra/README.md`, section 3) waits for the founder's approval of buying credit: staging's setup asks for no Anthropic key while its AI is off, and production's setup needs one. Both privacy notices were filled in on 17 September 2026 (the founder's name, contact address, and notes tool), because the pilot Worker serves them and refuses to start while one holds a bracketed blank.
2. Placeholders: staging's bot username and Hyperdrive id are filled in both wrangler files and committed. Production's `PLACEHOLDER_` values remain: the bot usernames and Hyperdrive ids in `apps/worker/wrangler.jsonc` and `wrangler.admin.jsonc` (the hosts and notice URLs are already filled). The setup script writes each from what Telegram and Cloudflare return, and the co-founder commits them (`infra/README.md`, section 11, step 8); nobody hands them over. The script also reads the founder's chat id from Telegram and puts it straight into the pilot Worker's secret `ADMIN_CONVERSATION_ID`. The notices were filled and regenerated (`pnpm --filter @vela/worker notices`) on 17 September 2026. Both Workers refuse to run while a placeholder is left.
3. **F** Staging: setup is complete and the basic smoke checks above passed. The founder has confirmed the first test-family exchange; next-morning reply read-back, the remaining live scenarios, the silence drill and watchdog verification remain. The setup command is `pnpm --filter @vela/worker run setup -- --env staging` in the founder's own terminal (`infra/README.md`, sections 11 and 12). It creates the queues and the dead-letter queue, and no R2 bucket, because staging's `MEDIA_STORAGE` is `off` (1.18; `infra/README.md`, section 1, step 3, says how to switch it on later); migrates the Neon project `vela-staging` (0.3); creates Hyperdrive `vela-apac-staging` with caching off and writes its id; writes the bot username and reads the chat id; puts each Worker's secrets; deploys `vela`, then `vela-admin`; waits while the founder has Cloudflare Access protect the whole `vela-admin` Worker, then puts its two Access secrets; registers the Telegram webhook on the pilot origin; and checks the result. Then the smoke test and the silence drill on staging (`infra/runbooks/release.md`, step 3; `infra/runbooks/silence-drill.md`). While staging's AI is off, no Worker reads an Anthropic key, so the drill's part D ("AI down") can test only a failed transcription, with the Deepgram key broken, as the note in part D says; its Anthropic half waits until staging's AI is switched on (item 4). Later staging deploys go through CI on merge to `main` (`release.md`). Staging never holds a real family (`infra/README.md`, "Environments"): only synthetic data and the founder's own test accounts. The one exception is set out in item 5: in the dogfooding week, a friend who is the kept-light member leaves their name, Telegram account and consent rows on staging until the week ends.
4. **F** Production, all in the founder's hands, since the co-founder never signs in to the "Vela" account. Production runs with AI on (`AI_PROVIDER` `anthropic`): its Workers refuse to start with AI off, because families' answers need the flag check, so the founder first approves buying Anthropic credit and creates the key (`infra/README.md`, section 3). Production also runs with media storage on (`MEDIA_STORAGE` `r2`, 1.18): the founder enables R2 in the "Vela" account before the setup, which asks for a payment method although the pilot's use stays inside the free monthly allowance, and the pilot Worker refuses to start with `off`, because the notice promises that voice notes and photos are kept in Vela's own storage for 30 days and then deleted (`infra/README.md`, section 1, step 3); the script's `resources` step then creates the bucket `vela-media-production`. Staging's AI is then switched on, and two checks run there before production's setup, so that no real family is the first to go through them: the silence drill's part D with the Anthropic key, and one scripted flagged answer with a health-words yes and one without (`infra/README.md`, section 3, step 7; pilot README, "Before any family"). Once staging has passed, the founder runs `pnpm --filter @vela/worker run setup -- --env production` on the commit proven on staging, with a setup token for the "Vela" account that expires the next day and is deleted after the run (`infra/README.md`, section 11, steps 3 and 8). The script does item 3's steps in the "Vela" account and on the Neon project `vela` (Hyperdrive `vela-apac`), and its deploy is the one production deploy from a laptop (`infra/README.md`, access rules). Then the 30-minute watch (`release.md`, step 7). Every later production deploy goes through a `v*` tag the founder approves (`release.md`, steps 4 to 6).
5. **F** The dogfooding week on staging, starting once item 3 has passed (1.10; decided 18 September 2026). The founder is the organiser, and a second Telegram account of the founder's, or a friend living in Taiwan who agrees, is the kept-light member. A friend takes part only once the founder has finished the C9 tasks, the providers' data processing terms still open in `plan/materials/pilot/legal-memo.md`'s status table (`infra/README.md`, "Founder tasks: data processing terms"); until then the kept-light member is the founder's second account. A friend uses only scripted test content, the words, photos and voice notes the founder gives them, never their own news, voice or health, so the week holds only synthetic data and a friend's name, Telegram account and consent rows, which the founder deletes from staging when the week ends (pilot README, "Before any family"). It exercises onboarding, consent, the health-words question, arrivals, answers, the quiet ladder, the weekly read, stop and start, the admin page, and the watchdog. AI is off on staging, so the week has no summaries, flag notices, translations, chips, or AI-written weekly read lines, and its definition of done does not require them (1.10); switching staging's AI on needs the founder's approval of buying Anthropic credit, then the steps in `infra/README.md`, section 3, step 7. The founder's own parent is not onboarded, for two reasons in `plan/materials/pilot/legal-memo.md` Q6: Russia's Federal Law 152-FZ reaches a foreign individual who processes a Russian citizen's data by consent, and since Law 23-FZ of 28 February 2025 forbids storing it in databases outside Russia, while Vela stores in Singapore; and Telegram has been largely inaccessible in Russia since mid-March 2026, so arrivals would not arrive reliably and quiet notices would fire on the platform's failure. The parent waits until there is a channel that works in Russia and a lawyer has advised on 152-FZ; Russia stays a deferred market (`plan/market-order.md`).
6. **F** The first real families, only after the dogfooding week passes and production is deployed (item 4): families living in Taiwan, the founder's friends and their families, each within "Who can join" in `plan/materials/pilot/README.md` (1.10). Before the first, the founder completes the checks in that README's "Before any family", including the providers' data processing terms still open in `legal-memo.md`'s status table (C9). Retention runs nightly in production, where the families' data is, and that is what keeps the notice's 30-day promise.

## Sprint 2 · Weeks 3–4 · Foundation and LINE

Goal: the platform ready for the app and for Taiwan.

| # | Task | Spec | DoD |
|---|---|---|---|
| 2.1 | **In progress, isolated API; not deployed.** API v1: Clerk auth, families/members/invites/nearby, exchanges, lights/quiet/away, media URLs and shared Zod contracts. Built locally: JWT and online session checks; scoped `/v1/me` and family-plan reads; optional idempotent POST `/v1/me/provision` and PATCH `/v1/me`; bounded JSON/error handling; actor-scoped replay receipts, quota and expiry cleanup; internal account-disable tombstones and cache purge; account linking proved by a code the member receives in their own Telegram chat, single use, fifteen minutes, five attempts, with nothing calling it yet. Writes default off; no profile call grants membership. Migrations `0001_api_request_receipts` and `0002_account_linking` are generated but not applied. An opt-in drill checks the receipt and account-write locking against a real PostgreSQL 18 across independent connections (`pnpm --filter @vela/services run test:postgres`); it found and we fixed a nightly-retention deadlock, and it passes whole. Remaining: real Clerk/gateway configuration and smoke tests, verified lifecycle/full-erasure integration, the route and bot command that would expose linking with its consent copy, deployment wiring, other endpoint permissions/consent, remaining contracts/routes, pagination and founder API collection. See API contract §1 for activation gates | api-contract | Contract tests for every route; Postman/Bruno collection for the founder |
| 2.2 | Region router and Hyperdrive bindings for apac, eu, us; Neon projects for eu and us created but idle | arch §12 | A family created with country DE lands in the eu database |
| 2.3 | LINE adapter: follow via invite link, account linking, arrival with quick replies and audio (m4a via R2), audio download from `api-data.line.me`, postbacks, unfollow, quota tracking on the paid Standard plan; audio through R2 needs `MEDIA_STORAGE` `r2` in that environment (1.18) | arch §8 | Founder's LINE test account completes the loop; quota shown in admin |
| 2.4 | Traditional Chinese strings for the bot path and the consent text; native reviewer sign-off | §20 | Reviewer approves; Lingui catalog `zh-TW` complete for the messenger path |
| 2.5 | STT benchmark: 30 consented pilot clips, kept in the private R2 bucket `vela-benchmark` in the "Vela" (production) account, because they are real families' consented personal data (data map row 23 and gap 19), through Deepgram, gpt-4o-transcribe, SenseVoice (Groq/Whisper as baseline); WER table in the admin | arch §9.2 | Default chosen on measured WER; ADR-14 updated |
| 2.6 | TTS pipeline: Azure zh-TW/en voices pre-rendered at compose time into R2, which needs `MEDIA_STORAGE` `r2` in that environment (1.18); `expo-speech` fallback path defined | arch §9.2 | Read-back plays as a file on LINE |
| 2.7 | Weekly read v0 for every live family, LINE included (AI draft, founder edits and sends from the admin page, as in 1.14) | §13 | Every live family's organisers receive a read on Sunday of week 4 |
| 2.8 | **Built** Retention job `applyRetention` (nightly, per region) implementing the data map's pilot rules (`plan/materials/pilot/data-map.md`, ADR-24): after 30 days clear `exchanges.text` and `options` (30 days after delivery), chips, translations, `replies.text`, the text in `answers.payload`, `answers.transcript`, `mentions`, `mood_words`, `flag_reason`, suggestions' text, `outbound.payload` (30 days after `sent_at`), `ai_calls.output`, and the reply text in `quiet_events.ask_to_check`; delete `message_refs` older than 30 days, invites 30 days after expiry or acceptance, and expired onboarding sessions; delete media per `expires_at` and `kept`, removing the id from `exchanges.media_ids` and `options`, with a `deletions` row for every media deletion; delete members 30 days after `left_at` and families within 24 hours of `deleted_at`; delete `events`, `metrics_daily`, `ai_calls`, and `outbound` rows older than 24 months; clear the Durable Object storage of deleted members (stop, left, and deceased clear it at once, 1.3); keep summaries, the flag boolean, and away dates while the family uses Vela. Retention tests for every rule; restore drill #1. Not yet done: restore drill #1, and a run per region, which waits for the region router (2.2); until then the Worker's nightly cron runs it on `apac` | §17 | In a test family moved past each window, every cleared column is empty and every deleted row is gone while summaries, flags, and away dates remain; a `deletions` row exists for each deleted media file; restore drill documented |
| 2.9 | **F** Five Taiwanese families recruited; first three live on LINE by week 4; a native reviewer for zh-TW | market order | Three LINE families answering |

DoD: instrument families migrated onto the API-backed platform with no missed arrival; the first LINE family live; STT default decided from real audio.

## Sprint 3 · Weeks 5–7 · The family app

Goal: organisers use the app for asks, exchanges, and the quiet notice.

| # | Task | Spec | DoD |
|---|---|---|---|
| 3.1 | **Sign-in works, 24 September 2026.** Expo app skeleton in `apps/app`: SDK 57 (the current release; the plan said 55 before the app existed), Expo Router with the four tabs, the Candle & Ink tokens and the light glyph in its five states, Literata and Inter, and Today (A6), which reads the API when the app is pointed at one and its fixtures otherwise (3.3). Clerk carries sign-in: a number **or** an email, whichever the account has, and a six-digit code to the same place; the founder signed in and saw their own seeded family. The workspace installs hoisted (`pnpm-workspace.yaml`), which React Native needs. Remaining: Apple and Google sign-in, Noto fallbacks, Lingui en + zh-TW, Zustand, Sentry, PostHog | design-system | Dev client on the founder's phone; sign-in works |
| 3.2 | **Built 24 September 2026: A1, A4, A5.** A signed-in account with no family is taken to onboarding. A1 asks her name, how Vela greets her, her language, where she lives and when she wakes, with her morning's hour shown as it changes; `POST /v1/families` makes the family and her invitation with Telegram onboarding's own helpers. A4 hands the organiser the words to send her, in her language, to share or copy. A5 shows the light waiting, and Today shows her as waiting for her yes until she gives it. Her consent is the bot's, unchanged. Not built: A2's first ask (nobody may ask her before she says yes; Ask does it after), A3's nearby contacts (with the Vela Light screens), and LINE and WhatsApp as her channel | §14.1 | Founder creates a family from the app end to end |
| 3.3 | **Built 23 September 2026 (reads).** A6 Today: the lights row, today’s exchange card with her answer, the replies in words and the receipt chip, tomorrow’s turn with its suggestion, and “Ask Mom something”. `GET /v1/families/:id/today` answers all of it (`api-today.ts`), each member’s day read in her own time zone, and the app falls back to its fixtures when it has no API to talk to. **Built 25 September 2026 (the API on a Worker):** the pilot Worker serves the API under `/v1` wherever its `API_V1` is `on`, development and staging, and not in production (ADR-29), with writes, the live session check, admission limits and the after-commit nudges on the real queue and scheduler; `pnpm --filter @vela/app start:staging` points the app at staging. Staging deploy: waits for the founder's key rotation and staging migrations 0001–0002 (infra/README.md §9a). Remaining: a suggestion writer, and Use this composing the ask | §14.1 | Matches the prototype; no badge counts anywhere |
| 3.4 | **Built 23 September 2026 for words-only asks.** A7 Ask: the prompt card with "Use this", the type grid, the free-text field with its preview line, and When. `POST /v1/families/:id/exchanges` composes it (`api-asks.ts`), one ask per morning held by her member row, and a claimed morning answers 409 with who holds it and the day to offer instead, which is what the screen shows. The family's Telegram group is told, in one line without the ask's words, when an app ask takes tomorrow morning. Remaining: the two-photo picker, the voice hello, a vote's options field, and the real translation preview | §14.1 | An ask composed in the app arrives on LINE next morning |
| 3.5 | **Built 24 September 2026 for words.** A8 Exchanges: the list newest first from `GET /v1/families/:id/exchanges`, delivered days only, thirty days back and then the family book, paged on the exchange id; one exchange opens with a reply composer that writes through `POST /v1/exchanges/:id/replies`. The caption says whether the reply reaches her, because only her latest day is read back. Remaining: voice and photo replies, reactions (which need the Telegram path to stop treating a member's reactions as its own), her originals (no translations route yet), and `GET /v1/exchanges/:id` for a link straight to one exchange | §14.1 | Replies read back to the parent next morning |
| 3.6 | **Built 24 September 2026 for the organiser's own answers.** A11 Quiet notice: the sheet opens itself over Today once for each quiet morning, for the family's organisers, and a tap on the quiet light brings it back. It reads `GET /v1/quiet/:id` — her usual hour, when the ask reached her and was asked again, when she last answered, and the people nearby who said yes, each with Call. She's fine and Wait 2 hours write through the same functions as the Telegram buttons, and the sheet then shows how it was settled; the messages to the others who were told go out after the commit, with `reconcile` behind them. A learning-period event, which tells nobody for eight hours, already shows in the app. Ink on surface, never red. Remaining: Ask them to look in (`ask-to-check`, which needs the actor stamp) and the precision page's `useful` verdict | §14.1, §8 | Silence drill part 1 visible in the app; a nearby ask carries the actor |
| 3.7 | **A12 and A13 built 24 September 2026; A14 not started.** A12 You reads `GET /v1/families/:id`: your name, role, language and city; your own light, shown where it stands; the family — each kept-light member with her light and where her Vela Light trial or plan stands, everyone else on one line, and for organisers the people nearby with whether each has said yes, never their numbers; One moment a day; and what the family sees about you. Pause and Leave work for oneself, with a confirm before leaving; neither may take away the family's last active organiser, and a kept light pauses in her own chat. Remaining: your own light and One moment a day, shown as they stand but not yet changeable (the first needs somewhere your own mornings arrive in the app, the second push, 3.8). A13 Vela Light is built: the first time an organiser sees her lit it opens once, with the price the plan will have, what is always free, and that nothing is charged in the pilot; "Start the 30 days" writes her trial through `POST /v1/families/:id/plan/trial`, and You opens it again. Remaining: A14 widget (iOS via expo-widgets with Swift fallback, Android widget) | §14.1 | Widget shows the light and updates on answer |
| 3.8 | Push: Expo push tokens, one-a-day budget, time-sensitive quiet notice on iOS | §15 | Exactly one push per member per day in a test week |
| 3.9 | Maestro flows: onboarding, ask, reply, quiet notice; TestFlight + Play internal builds via EAS | arch §16 | Founder installs from TestFlight |
| 3.10 | **F** Fifteen families across Taiwan and English-speaking countries; UCLA-3 at week 4 for the first cohort; decide the entity | plan | Ten families composing asks in the app |

DoD: ten families use the app for asks; answer rate ≥ 75% across kept-light members; one push per day verified.

## Sprint 4 · Weeks 8–9 · The parent surface

Goal: a parent with no messenger uses the app's parent surface, installed by a visiting child.

| # | Task | Spec | DoD |
|---|---|---|---|
| 4.1 | Parent-surface mode: device-bound token issued by an organiser; light mode only; StyleSheet kit with the accessibility checklist | §14.2, arch §10 | Passes the checklist at the largest accessibility font size |
| 4.2 | P1 consent, P2 question (chips primary, mic below, read-back first), P3 photo choice, P4 recording (expo-audio, meter, playback, send/again), P5 answered (light blooms once, replies read aloud, photos, call) | §14.2 | Two parents complete a week each |
| 4.3 | TTS read-back in English and Chinese with on-device fallback; ask audio pre-rendered | arch §9.2 | Works with the network off (fallback voice) |
| 4.4 | P6 kitchen-table mode: landscape, keep-awake, photo cycling, one chime, tap to answer; tested on a 2019 Android 8 tablet | §14.2 | Runs 24 h on the tablet without jank |
| 4.5 | Voice-line design (Twilio Studio flow, consent capture, legal notes per market) documented, not built | arch §8 | Design reviewed; US listed first |
| 4.6 | **F** Two parents on the parent surface via a visiting child (one abroad, one in Taiwan) | plan | Both answered 5 of 7 days |

DoD: parent surface answered by two parents for a week each; accessibility checklist signed.

## Sprint 5 · Weeks 10–11 · Depth and the second wave

| # | Task | Spec | DoD |
|---|---|---|---|
| 5.1 | Story day and the family book (A10): question bank (52, en + zh-TW), voting, keep/don't keep, in-app reading; PDF export job behind the Light flag | §10 | A story recorded on Sunday appears in the book |
| 5.2 | Weekly read in-app (A9) from the batch AI call with the free/locked state; suggestion; story of the week | §13 | Organisers open the read; Sunday batch job runs |
| 5.3 | Memory facts and reminder suggestions (only after a tap); recipe cards | §12 | A dated fact becomes a suggested reminder |
| 5.4 | Precision page (notices, outcomes, useful verdicts) | §8 | Page live in the app and admin |
| 5.5 | WhatsApp Cloud API adapter in the sandbox (utility template drafted, interactive replies, statuses, opt-in in the app); Meta verification started if the entity exists | arch §8 | Sandbox loop works; template submitted |
| 5.6 | Japanese localisation scoped; `ja` catalog skeleton; Azure ja voice tested | market order | Scope document; a ja read-back plays |
| 5.7 | Field-level encryption decision for transcripts (ADR); restore drill #2; load test at 10× | arch §13, §16 | ADR written; k6 report |
| 5.8 | **F** Ten Taiwanese families on LINE; NT$ pricing test designed; pricing page copy | plan | Ten LINE families answering |

DoD: family book and weekly read live; WhatsApp sandbox sending; precision page public inside the app.

## Sprint 6 · Week 12 · Measure and decide

| # | Task | DoD |
|---|---|---|
| 6.1 | Metrics review against spec §18 targets: answer rate weeks 1–4 and week 12, latency, content share, replies per answer, quiet notices per month and usefulness, stop rate, quiet-day rate, minutes per day. §18's paying target and its kill signal (families paying at day 90, [< 4%]) apply once billing is open; while the pilot is free, the day-30 answer (6.2) stands in for them | Report in the master plan |
| 6.2 | UCLA-3 at week 12; "if Vela stopped tomorrow" at day 30 for every organiser; willingness to pay asked on the same day-30 call: "Would you pay for Vela Light at US$9.99 a month?" (the pilot is free and billing is not open) | Report |
| 6.3 | Trial flow ready; billing provider chosen the moment the entity exists (RevenueCat + web checkout where allowed) | Decision recorded |
| 6.4 | Store readiness: listings in en and zh-TW, health disclaimer, accessibility label, privacy labels, D-U-N-S in hand | Checklist complete |
| 6.5 | Decide: open billing and a public Taiwan launch, or change one variable and rerun | Decision in the master plan |

## Cross-cutting, every sprint

- CI green on every merge; Promptfoo on every prompt change; contract tests on every adapter change.
- The silence drill runs nightly from sprint 1.
- Weekly: cost check across Cloudflare, Neon, Anthropic, Deepgram, LINE; a 30-minute founder review of the admin page; the golden set grows by ten real, consented cases.
- Every decision that changes an ADR is appended to `architecture/decisions.md` the same day.

## Kill signals during the build

Answer rate under 50% by week 4 of any cohort; stop rate over 30% in month 1; fewer than 4 organisers saying on their day-30 call that they would pay for Vela Light at US$9.99 a month; a double send or a false quiet notice attributable to us that the tests did not catch (stop and fix before any new family).
