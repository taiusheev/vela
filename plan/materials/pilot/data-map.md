> **Draft for the pilot, not legal advice. To be reviewed by counsel before any public launch.**

# Data map

Version `data-map.v1` · 17 September 2026 · for the founder, the co-founder and counsel · sources: spec §17, architecture §5 and §12, `architecture/schema.sql` with the changes in `architecture/03-code-design.md` §3, `architecture/04-instrument-flows.md` and the sprint 1 contract decisions of 14 September 2026 (`architecture/03-code-design.md` §10; `architecture/decisions.md` ADR-21 to ADR-25), the hosting decision of 17 September 2026 (ADR-26), the founder's decisions of 17 September 2026 (a free pilot, the notes tool, the benchmark bucket's account), `infra/sub-processors.md`

Every category of personal data Vela handles in the pilot: where it comes from, where it lives, why, how long, who can see it, and which sub-processors touch it. The privacy notice (`privacy-notice.en.md`) is the plain-language version of this table; when one changes, change the other in the same commit.

**Retention column.** *Defined* means a source document sets it (quoted). *Pilot rule* means the sources left it open and this pack set it. ADR-24 puts this pack's rules for Vela's database and Durable Object storage into the retention job (`applyRetention` in `@vela/services`, flows §3.15, build plan 2.8), except the 24-month limit on `admin_access_log` (gap 18). Until that job ships with its retention tests, the privacy notice is not used with a family beyond the founder's own. See "Gaps" at the end.

**Who can see.** Family = members of the family's Vela group · Organisers = organisers of that family · The person = the kept-light member · Contact = a nearby contact · Founder = the founder through the admin page (the admin Worker `vela-admin`, behind Cloudflare Access, ADR-26; every view and every admin action logged in `admin_access_log`, flows §3.17) or the Neon console (logged by hand, `infra/runbooks/data-requests.md`); the admin conversation on Telegram carries no family content (row 25, ADR-21) · Co-founder = the AI coding agent, content-free data only (see `infra/README.md`, access rules).

**Sub-processors.** CF = Cloudflare (the Workers `vela` and `vela-admin`, Durable Objects, Queues, R2, Hyperdrive, Access) · Neon · Anthropic · Deepgram · Sentry · TG = Telegram · LINE (from sprint 2) · Google = Google Drive in the founder's own Google account, holding the founder's notes in a private folder "Vela pilot" shared with no one (Google's servers in the United States and other countries). Details and regions: `infra/sub-processors.md`.

## In use in the pilot (sprints 1 and 2)

| # | Category | Examples | Source | Where it lives | Purpose | Retention | Who can see | Sub-processors |
|---|---|---|---|---|---|---|---|---|
| 1 | Member identity | Display name, role, language, time zone, country | The member through the messenger; organiser at onboarding | `members` (display_name, role, language, tz, country, status, turns_in); `families` (name, country, region, language) | Address people, route turns, pick the language | *Defined:* member `left` → deleted 30 days later; family deleted → within 24 h (spec §17, schema). *Pilot rule:* a member with role `member` whom Vela knows (linked when they first wrote in the group) becomes `left` when they leave the family group, and leaves the turns; an organiser or the person the light is for who leaves the group keeps their state, and the founder is told (`admin.member_left_group`); someone Vela never linked leaves nothing stored (flows §3.16). The founder marks anyone else who stops taking part as `left` (`mark_left`, flows §3.17; by hand in `infra/runbooks/data-requests.md` F until the admin page ships). End of pilot without continuing: the founder requests the family's deletion within 30 days (`delete_family`, flows §3.17) | Family (names), Founder | Neon, CF |
| 2 | Profile of the person the light is for | Name the family uses, address form, wake time, arrival time, age band, light on, consent time | Organiser; confirmed by the person on the call | `members` (display_name, address_form, age_band, wake_time, arrival_time, light_on, light_consented_at, light_consent_text) | Deliver at their hour, greet them their way | As row 1 | Family (name, greeting), Organisers, Founder | Neon, CF |
| 3 | Rhythm and scheduler state | Answer latency statistics, quiet threshold, learning period, next wake time; the Durable Object's pending wakes | Derived by Vela | `members` (answer_stats, quiet_after_min, learning_until, next_wake_at); `MemberScheduler` Durable Object storage (member id, time zone, arrival time, pending wakes) | Wake at the right minute; tune the quiet ladder (spec §8) | Columns: as row 1. *Pilot rule:* Durable Object storage and alarm cleared when the member stops, leaves, is deleted or is marked deceased | Organisers (as light state), Founder | Neon, CF |
| 4 | Channel identifiers | Telegram user and chat ids, username, family group id, platform message ids, onboarding step data, invite tokens | Telegram (and LINE) | `channel_links`, `family_channels`, `message_refs`, `onboarding_sessions`, `invites` | Deliver messages; resolve replies and button taps to the right exchange | `channel_links`: as row 1. `onboarding_sessions`: until `expires_at`. *Pilot rule:* `message_refs` 30 days; `invites` deleted 30 days after `expires_at` or acceptance | Founder | Neon, CF, TG, LINE |
| 5 | Consent records | Kind (light, nearby, privacy_notice, pilot), text version, language, channel, time, evidence (message id, the person's words), withdrawal | The person; the founder's call note; `pilot`, `privacy_notice` and `nearby` answers are recorded by the founder on the admin page once the member or contact row exists (`record_consent`, `record_contact_consent`, flows §3.17; by hand in `infra/runbooks/data-requests.md` B and C until it ships) | `consents`; the founder's notes | Prove agreement and honour withdrawal (architecture §12) | *Defined:* cascades with the member or contact row. *Pilot rule:* kept as long as the data it covers | Founder; the person on request | Neon, Google |
| 6 | Nearby contacts | Name, relation, phone, messenger, consent requested, given or declined | The organiser (indirect collection, PDPA Art. 9), at Vela's setup or through the founder | `nearby_contacts` (`consented_at`, `declined_at`); `consents` kind `nearby` | Give organisers the people to call on a quiet day | *Pilot rule:* stored without consent (`consented_at` null) when the organiser gives them at setup or the founder adds them (`add_contact`), and listed in a quiet notice only once their yes is recorded and no decline is (`quiet.nearby`, flows §3.1 and §3.12). The founder records the yes or no on the admin page (`record_contact_consent`, flows §3.17; by hand in `infra/runbooks/data-requests.md` C until it ships). Deleted (`remove_contact`) within 7 days of a no or a withdrawal, when no yes has been recorded 14 days after they were added, when the organiser removes them, or when the family leaves. App (sprint 3): deleted on decline or after 14 days without an answer | Organisers, Founder. Never the AI (spec §17). Never the person's answers to the contact | Neon, CF, TG (names and numbers appear as text in the organiser's setup chat and, after a yes, in the organiser's quiet notice in phase 0) |
| 7 | Asks | Question text, options, photos, a 10-second voice hello, type, when rule, a child's first name (`on_behalf_of`, which no pilot flow sets) | Family members | `exchanges` (text, options, media_ids, voice_hello_id, on_behalf_of, type, state and timestamps); `chips`; `translations` (object_type `exchange`) | Deliver the ask; offer tap answers | *Defined:* media 30 days (row 11). *Pilot rule:* text and options cleared 30 days after delivery, chips and translations deleted after 30 days (ADR-24); state and timestamps kept as row 1 | The person, Family, Founder | Neon, CF, Anthropic (chips, translate), TG |
| 8 | Answers | Kind (voice, chip, photo pick, vote, heart, text, photo, fine, sticker), payload, time received, channel, platform message id | The person | `answers` (kind, payload, channel, external_id, media_id, received_at); `media` + R2. A message sent when no ask was delivered in the last 36 hours gets no `answers` row: it is posted to the group through a delivery record (row 12; flows §3.9). Nothing the person writes before saying yes to the light, after saying no, or once marked left or deceased is stored or posted (flows §3.2, §3.9) | Carry the answer; light the light synchronously (architecture §8) | *Defined:* "answers' transcripts and media past 30 days unless kept" (architecture §12). *Pilot rule:* payload text cleared at 30 days; kind and time kept as row 1 | Family (as a post), Founder | Neon, CF, TG |
| 9 | What the AI derives from answers | Transcript and its language, one-line summary, mood words, mentions (people, places, plans, **health words**, dates), flag and flag reason (a verbatim quote), away until | Deepgram and Anthropic, from rows 7 and 8 | `answers` (transcript, transcript_lang, summary, mood_words, mentions, flag, flag_reason, away_until, understood_at, processing_attempts); `translations` (object_type `answer`) | Family understanding across languages, organiser's day note, flags (spec §5.5), away mode. An answer not understood is re-run up to three attempts in all (flows §3.10 and §3.15, ADR-25) | *Defined:* transcript 30 days; summaries kept (spec §17, architecture §12). *Pilot rule:* mentions, mood words and flag reason cleared at 30 days, translations deleted at 30 days; summary, flag boolean and away dates kept as row 1 | Family (transcript, translation), Organisers (summary, flag quote), Founder (on the admin page; the admin conversation carries only a link, row 25) | Deepgram, Anthropic, Neon, CF, TG (transcript and translation posted to the group; flag quotes sent to organisers) |
| 10 | Replies and reactions | Heart, laugh, hug, text, voice, photo; to whom; read-back time | Family members | `replies` (kind, text, media_id, to_recipient, channel, external_id, read_back_at); `translations` (object_type `reply`) | Read the family's words back to the person the next morning (spec §6) | *Defined:* media 30 days. *Pilot rule:* text cleared and translations deleted at 30 days; kind and times kept as row 1 | The person (read-back), Family, Founder | Neon, CF, Anthropic (readback), TG |
| 11 | Media files | Voice notes (m4a, ogg), photos | The person and family members | R2 bucket `vela-media-production` ("Vela" account; staging's `vela-media-staging` holds synthetic media only); `media` (storage_key, channel, provider_file_id, provider_unique_id, mime, bytes, duration, kept, expires_at). A file forwarded again inside one family reuses its row (flows §3.5) | The exchange; the family book | *Defined:* "30 days unless kept" (`expires_at`, `kept`); every deletion writes `deletions`, and the retention job removes the deleted id from `exchanges.media_ids` and `exchanges.options` (flows §3.15) | As the ask, answer or reply it belongs to | CF (R2), Deepgram (audio for transcripts), TG, LINE |
| 12 | Delivery records | Rendered message payload (contains the message text), kind, channel, status, attempts, error, platform id, actor for nearby asks; queue jobs | Vela's gateway | `outbound` (payload, idempotency_key, actor_id, status, external_id, error); Cloudflare Queues (`vela-outbound-production`, `vela-media-production`, `vela-understand-production` and the dead-letter queue `vela-dead-letter-production`: ids only) | One message per kind per day, retries, proof that a person sent every nearby ask (spec §8) | *Pilot rule:* payload cleared 30 days after `sent_at`; the rest kept 24 months, or deleted earlier with the member (`outbound.member_id` cascades). Queue messages: platform retention, ids only | Founder | Neon, CF |
| 13 | Quiet days and away | Opened, notified, wait until, ask-to-check entries (contact, sender, time, reply), resolution, outcome, useful verdict; away periods and who set them | Vela; organisers; the person's own words ("until Sunday") | `quiet_events` (with `notified_member_ids`, `notify_count`, `last_notified_at`), `away_periods` | Keep the light (spec §8); precision accounting | *Defined:* "precision outcomes" are kept (spec §17). *Pilot rule:* reply text inside `ask_to_check` cleared at 30 days; the rest as row 1 | Organisers, Founder | Neon, CF, TG |
| 14 | Turns and suggestions | Turn holder, suggestion text drawn from the person's recent mentions | Vela; Anthropic | `turns`, `suggestions` | Rotate asking; help the turn holder (spec §7) | *Pilot rule:* suggestion text cleared at 30 days; turns kept as row 1 | Turn holder (the group sees whose turn it is), Founder | Neon, Anthropic, CF, TG |
| 15 | Weekly reads | The week's counts (days counted and answered, mornings nobody in the family asked, the family's asks) and other stats; the draft lines and suggestion; the lines and suggestion as the founder sent them | Vela (the counts and stats); Anthropic draft of the lines and suggestion, from the days the person answered only, edited by the founder on the admin page, where the counts cannot be edited | `weekly_reads` (`stats` holds the counts; `lines` and `suggestion` are the draft; `sent_lines`, `sent_suggestion` and `sent_at` record what was sent); `translations` (object_type `weekly_read`: the sent lines in the person's language, written at Send when the family's language differs); the sent message as a delivery record (row 12) | Sunday read to organisers (spec §13): the counts, rendered from numbers, then the lines and the suggestion, sent by Vela to each active organiser when the founder taps Send (flows §3.14, §3.17); the lines shown to the person in their language | *Defined:* kept (spec §17). *Pilot rule:* as row 1; translations deleted at 30 days | Organisers (the counts, lines and suggestion), the person the light is for (only the lines of the most recent sent read, in their language, in "what does the family see"; never the counts or the suggestion, spec §8, flows §3.13), Founder | Anthropic, Neon, CF, TG |
| 16 | AI call log | Call, prompt version, model, tokens, cost, latency, input references, **output** (summaries, flag quotes, chips, translations) | Vela | `ai_calls` | Audit and compare prompt versions (ADR-8); cost | *Defined:* none ("inputs are never copied"). *Pilot rule:* `output` cleared at 30 days; the rest kept 24 months, with `family_id` and `member_id` set to null when those rows are deleted | Founder; co-founder sees metadata only | Neon |
| 17 | Usage events and daily metrics | Event name, family, member and exchange ids, surface, local time, kinds, durations, outcomes; daily counts | Vela | `events`; `metrics_daily` | Measure the loop (spec §18); operations | *Defined:* events 24 months (architecture §12). *Pilot rule:* `metrics_daily` 24 months too (ADR-24) | Founder, co-founder (content-free) | Neon |
| 18 | Admin access log | Who opened or changed which family's records, about which member, with which action (`view` or an admin write action, flows §3.17), when, and a detail that never holds message content, names or phone numbers | Vela, for every admin page view (one row for each family the page shows, so the overview of all families writes one per family) and action; the founder, by hand, for the Neon console | `admin_access_log` (admin, family_id, member_id, action, what, at); `family_id` and `member_id` have no foreign key, so a row outlives the family and member it names | Accountability; visible to organisers on request (architecture §11) | *Pilot rule:* 24 months; not yet in the retention job (gap 18) | Founder; organisers on request | Neon |
| 19 | Deletion proofs | Object type and id, content hash, reason | Retention jobs | `deletions` | Provable deletion without keeping content (architecture §5) | *Defined:* kept | Founder | Neon |
| 20 | Logs and error reports | Event names, record ids, error messages, stack traces. Never message content, transcripts, phone numbers or tokens (code design §2) | The Workers | Workers Logs of `vela` and `vela-admin`; Sentry project `vela-worker` (not connected to the Workers yet) | Find and fix failures | Workers Logs: Cloudflare's default. *Pilot rule:* Sentry at most 90 days, IP storage off | Founder, co-founder | CF, Sentry |
| 22 | Pilot research | Onboarding call notes, UCLA-3 answers at weeks 0, 4, 12, "if Vela stopped tomorrow" at days 14 and 30, whether the organiser would pay for Vela Light at US$9.99 a month (day 30), quotes | Calls, with a separate yes | Google Drive in the founder's own Google account: the private folder "Vela pilot", shared with no one, filed under a family code | Pilot evidence, including willingness to pay while the pilot is free (spec §18, Appendix A; build plan 1.10, 6.2) | *Pilot rule:* until the pilot report is written; deleted at most 12 months after the pilot ends | Founder | Google |
| 23 | Eval and benchmark items | A consented answer, anonymised into text for the golden set; consented voice clips for the STT benchmark (build plan 2.5) | The person, with a separate yes per item | Golden set: `packages/ai/evals/` in git (text only, names and places replaced). Clips: outside git, in the private R2 bucket `vela-benchmark` in the "Vela" (production) Cloudflare account, which the co-founder never signs in to, created in sprint 2 before the first clip (`infra/README.md`; gap 19) | Improve AI quality (architecture §9.3) | *Pilot rule:* golden-set text is anonymous before it is committed (git history cannot be deleted); clips deleted within 30 days after the benchmark | Founder; co-founder (anonymised text only) | GitHub (anonymised text), CF (R2, benchmark clips), Deepgram, OpenAI and Groq (benchmark clips, sprint 2) |
| 24 | Copies on the messaging platforms | The chat history in the family group and the private chats of the person and the organisers | Everyone in the chats | Telegram, LINE | Carry messages | The platform's policy; deleting in Vela does not delete chat history | Chat participants | TG, LINE |
| 25 | The admin conversation | The family's name and a link to the admin page for a flag (`admin.flag`), a weekly-read draft ready to edit (`admin.weekly_read_draft`) and an answer not understood after three attempts (`admin.understand_failed`); the family's name and a member's name when an organiser or the person the light is for leaves the family group (`admin.member_left_group`). Never anything the family wrote (ADR-21) | Vela (flows §3.10, §3.14, §3.15, §3.16) | The founder's private Telegram chat with the bot (its id is `ADMIN_CONVERSATION_ID`, a secret of the pilot Worker `vela`, never in the repository), on Telegram's servers | Tell the founder to act; the founder reads and acts on the admin page, where every view is logged (row 18) | *Pilot rule:* the founder deletes a family's messages from this chat when the family is deleted (`infra/runbooks/data-requests.md` K) | Founder | CF, TG |

There is no row 21: the pilot is free for families, so Vela collects no payment data (decided 17 September 2026). The other rows keep their numbers because other documents cite them.

## Later in the build (not collected in sprint 1)

| # | Category | Where it will live | First sprint | Notes |
|---|---|---|---|---|
| 26 | Accounts | `users` (email, phone, auth_subject); Clerk | 3 | Organisers and members with the app; the person the light is for never has an account (ADR-13) |
| 27 | Push tokens and app analytics | Expo Push; PostHog Cloud EU (no content, architecture §15) | 3 | Needs a privacy notice version that names Clerk, Expo, Apple, Google and PostHog |
| 28 | Family book | `stories`, `recipes`, `media.kept = true` | 5 | *Defined:* kept until "don't keep that one" or the family leaves; export behind Vela Light. The notice and the organiser agreement describe it as coming later in the pilot; no instrument flow sets `media.kept` |
| 29 | Memory and reminders | `memory_facts` (kind includes `health`; `expires_at`), `reminders` | 5 | `expires_at` has no defined value; health facts need counsel's view on PDPA Art. 6 before this ships |
| 30 | Read-back audio | Azure TTS input text; rendered audio in R2 | 2 | Microsoft becomes a sub-processor; rendered audio follows the 30-day media rule |
| 31 | Billing | `subscriptions`; RevenueCat, app stores | after the entity | Not in the pilot |

## End of the pilot without continuing

Within 30 days the founder requests the family's deletion (`delete_family` on the admin page, flows §3.17, build plan 1.12), and the retention job deletes the family's rows within 24 hours of that request (flows §3.15, build plan 2.8). Until both have shipped, the founder deletes them by hand (`infra/runbooks/data-requests.md` K). Kept longer, each for its own period above: rows 16 (without names or output), 17, 18, 19, 20 and 22. The privacy notice lists the same exceptions.

## What the AI sees

Per spec §17 and architecture §12: the answer (text or transcript), the ask, the family's replies, the member list with first names, roles and languages, and memory facts. Never billing data, never nearby contacts' numbers, never the whole archive. Every call is logged in `ai_calls` with its prompt version (row 16).

## Gaps found while building this map

Each gap says what was found and, where the sprint 1 contract decisions of 14 September 2026 close it, how the design closes it (flows section and decision record) and when it is closed in practice (the build plan task that ships it). Until then the pack's manual steps stand.

1. **Retention the sources left open.** The schema's retention covered media, transcripts, left members, deleted families and events only, and flows §3.15 and build plan 2.8 listed no more, so the job as specified would not have kept the notice's 30-day promise for asks, replies, translations, chips, suggestions, `outbound.payload`, `ai_calls.output` or `message_refs`, nor deleted `metrics_daily` at 24 months. *Closed in the design:* ADR-24 and flows §3.15 put this pack's rules (rows 3, 4, 7–14, 16, 17) into `applyRetention`, and the schema's retention header lists them. A cleared NOT NULL column takes its empty value, and chips and translations rows are deleted, since they hold nothing but their text. *Closed in practice* when `applyRetention` ships with a retention test for every rule and runs nightly (build plan 2.8); until then the notice is not used beyond the founder's own family (`README.md`, "Before first use"). Still open: gap 18.
2. **Summaries kept while the family uses Vela** leave a derived trace of every answer until the family is deleted, which needed a deletion path at the end of the pilot. *Closed in the design:* the founder requests a family's deletion on the admin page (`delete_family`, flows §3.17, ADR-22) and the retention job deletes the family within 24 hours (flows §3.15, ADR-24). *Closed in practice* when build plan 1.12 and 2.8 have both shipped; until then, `infra/runbooks/data-requests.md` K.
3. **`metrics_daily` was "kept"** in the reference schema while architecture §12 limits events to 24 months; the notice promises 24 months for both. *Closed in the design:* ADR-24 deletes `metrics_daily` rows older than 24 months. *Closed in practice* when `applyRetention` ships (build plan 2.8).
4. **Consent proof** cascades away when a member is deleted, so proof of consent disappears with the data. Acceptable for the pilot; counsel to confirm whether proof must outlive deletion.
5. **Events partitioning.** Architecture §5 and §12 drop monthly partitions after 24 months; code design §3 makes `events` a plain table. Retention is a delete by `at`, not a partition drop (ADR-24).
6. **Unrelated group messages.** The bot's group privacy mode stays on (flows §1): asks are replies to the evening prompt, `/ask` or `/later`, which privacy mode delivers. But reactions count only when the bot is a group administrator, and Telegram delivers every group message to an administrator bot whatever the privacy setting. The notice says so and promises everything not meant for Vela is discarded without being stored or logged; the inbound handler must do exactly that (flows §5, "anything else: ignore"). The one service message Vela reads is `left_chat_member`, which every bot receives in privacy mode (gap 14).
7. **Golden set in git** (architecture §9.3, build plan "ten real, consented cases" weekly) conflicts with provable deletion (constraint 6): only truly anonymised text may be committed, and voice never.
8. **OpenAI and Groq** receive audio in architecture §9.2 and build plan 2.5 but are missing from the architecture §12 sub-processor list.
9. **Health words** in `answers.mentions`, `flag_reason` and `memory_facts` (kind `health`): whether these are "medical" data under PDPA Article 6 needs counsel's view before memory ships.
10. **Pilot research data** (UCLA-3, call notes, the day-30 answer on paying) lives outside the schema, so the tool holding it had to be named in the notice before use. *Closed* on 17 September 2026: it is Google Drive in the founder's own Google account, in a private folder "Vela pilot" shared with no one, filed under a family code (rows 5 and 22), and both notices name it and where its data is stored (Google's servers in the United States and other countries; a personal account offers no choice of location). Still open: a personal account comes with Google's own terms, not the data processing agreement `infra/sub-processors.md` asks for before real family data reaches a provider; counsel to confirm that notes under a family code may stay there.
11. **Nearby contacts at setup.** Flows §3.1 asked the organiser for nearby contacts during setup and inserted them without consent, and §3.12 listed every nearby contact in the quiet notice. *Closed in the design:* flows §3.1 keeps the setup step but stores contacts with `consented_at` null, and §3.12 lists in `quiet.nearby` only contacts with `consented_at` set and `declined_at` null, leaving the line out when there are none (code design §10, "Nearby contacts"); `onboarding.ask_nearby` tells the organiser to ask each contact themselves before their number can appear in any note (already in `@vela/copy`). The founder records each yes or no on the admin page (`record_contact_consent`, flows §3.17). *Closed in practice* when the sprint 1 services ship the quiet notice and the admin actions (build plan 1.8, 1.12); until the admin page ships, the founder records answers by hand (`infra/runbooks/data-requests.md` C). The design does not say what happens to a stored contact who says no or never answers; this pack removes them (row 6, data-requests D).
12. **Region.** Flows §3.1 set `families.region` from the country (`us`, `eu`), while only the `apac` database exists and the notice says the database is in Singapore. *Closed in the design:* ADR-7 (updated 14 September 2026) and flows §3.1: `Config.regions` lists the regions that exist (`apac` only in the pilot), and a country whose preferred region does not exist falls back to `apac`, so every pilot family is `apac`. *Closed in practice* when the sprint 1 services ship onboarding; the founder confirms the region after the first setups (`infra/runbooks/data-requests.md` A). Before the `eu` or `us` database holds anyone, the notice needs a new version.
13. **No admin write path.** The admin route was a read-only page and no flow wrote `pilot`, `privacy_notice` or `nearby` consents, organiser-set away periods, `deceased`, a member leaving or a family's deletion. *Closed in the design:* ADR-22 and flows §3.17 add admin actions (services module `admin.ts`; POST forms on `/admin` in the admin Worker `vela-admin`, behind Cloudflare Access with a verified Access JWT and a same-origin check, ADR-26): `record_consent`, `record_contact_consent`, `add_contact`, `remove_contact`, `set_away`, `end_away`, `mark_left`, `mark_deceased`, `delete_family` and `send_weekly_read`. Each writes an `admin_access_log` row with its `action` (and `member_id` when it is about one member) and records a domain event; opening a page writes action `view`. *Closed in practice* when the sprint 1 worker ships these forms (build plan 1.12, and 1.14 for `send_weekly_read`); until then the founder follows `infra/runbooks/data-requests.md`, logging each family touched by hand. Still manual afterwards: a new invite link, a copy of someone's information, corrections, and deleting one family member's information on request (data-requests A, H, I, J).
14. **Departures.** Vela received no update when someone left a group, so nothing set `members.left_at`. *Closed in the design:* Telegram delivers the `left_chat_member` service message to bots even in privacy mode, inside `message`, so the webhook's `allowed_updates` needs no `chat_member` (flows §1), and `@vela/adapters` already turns it into a `member_left` event naming who left. In a linked group, a linked member with role `member` who is not the person the light is for becomes `left` with `left_at` and leaves the turn rotation (event `member_left`); an organiser or the person the light is for keeps their state, event `member_left_group` is recorded and the founder receives `admin.member_left_group`; someone with no link leaves nothing stored (flows §3.16; code design §10, "Departures"). *Closed in practice* when the sprint 1 services ship it (build plan 1.11). Not verified: that Telegram posts the service message in every kind of group; the founder still marks a member left when told (`mark_left`).
15. **The admin conversation** (row 25) held verbatim flag quotes and weekly-read drafts outside `admin_access_log` and outside `applyRetention`, and the pack relied on the founder deleting them within 30 days. *Closed in the design:* ADR-21: `admin.flag`, `admin.weekly_read_draft` and `admin.understand_failed` carry the family's name and a link to the Access-protected admin page (`Config.publicBaseUrl` + `/admin/families/<family id>`), never her words, and opening the page writes `admin_access_log` (flows §3.10, §3.14, §3.15, §3.17). The copy keys are already changed in `@vela/copy`. Organisers still receive `flag.notice` with her words. *Closed in practice* when the sprint 1 services and worker ship (build plan 1.9).
16. **AI failures were not retried.** `@vela/ai` resolves every provider error to its safe default, and nothing ran the flag check again. *Closed in the design:* ADR-25 and flows §3.10, §3.15: `answers.understood_at` is set only when `ai.understand` and `ai.flag` both succeed (a failed translation does not block it); `answers.processing_attempts` (already in `@vela/db`) counts each start of media ingestion or understanding, so a voice answer's first run counts two when its transcription succeeds; every 5 minutes `reconcile` re-enqueues answers not understood that arrived between 15 minutes and 24 hours ago with fewer than three attempts, and the founder receives `admin.understand_failed` once after the third failed attempt. Re-runs never repeat the group transcript post, translations or flag notices. *Closed in practice* when the sprint 1 services ship it (build plan 1.13); until then, and for any answer that ends in `admin.understand_failed`, the founder reads it by hand (`infra/runbooks/incident.md`).
17. **What the family sees.** Spec §9 promises the last weekly read with the summaries; flows §3.13 returned summaries only, and `weekly_reads` did not hold the text the founder sent. *Closed in the design:* the founder edits the draft on the admin page and taps Send (`send_weekly_read`); `weekly_reads.sent_lines`, `sent_suggestion` and `sent_at` (already in `@vela/db`) keep the lines and suggestion as sent, Vela sends them to each active organiser after the counts it renders from `weekly_reads.stats`, and "what does the family see" returns the summaries of her last seven answered days and the lines of the most recent sent weekly read, never its counts or suggestion (`parent.family_sees_weekly_read`; flows §3.13, §3.14, §3.17; code design §10, "Weekly read"). The notice now says so. *Closed in practice* when the sprint 1 services and worker ship it (build plan 1.14).
18. **The admin access log's 24 months** (row 18) is not in ADR-24's list, so no job deletes older rows. Nothing is due before September 2028; the retention job needs the rule, or the founder deletes older rows by hand, before then.
19. **Benchmark clips' account** (row 23). The consented voice clips were to go to the private R2 bucket `vela-benchmark` in the "Vela staging" Cloudflare account. That is the account the development machine's Wrangler sign-in reaches, and a Wrangler sign-in covers every bucket in its account (`infra/README.md`, access rules), so for these clips the rule that the co-founder never handles real family content would have rested on policy, not on the account, and staging would have held real voices. *Closed* on 17 September 2026 by the founder: the clips are consented personal data from real families, so `vela-benchmark` is created in the "Vela" (production) account, which the co-founder never signs in to, and the clips are out of the co-founder's reach like every other place real family content lives (build plan 2.5).
