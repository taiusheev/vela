# The staging API (ADR-29). Read only by `pnpm --filter @vela/app start:staging`, which loads
# this file before Expo does; Expo never loads it by itself. The publishable key still comes
# from .env.local.
EXPO_PUBLIC_API_URL=https://vela.vela-light-staging.workers.dev
