{
  "name": "@vela/ai",
  "version": "0.0.0",
  "private": true,
  "description": "Claude calls with versioned prompts and structured outputs, speech-to-text, and fakes for tests.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "eval": "node evals/gate.ts preflight && pnpm dlx promptfoo@0.123.0 eval --config evals/promptfooconfig.json --output evals/results/latest.json && node evals/gate.ts check"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "catalog:",
    "@vela/contracts": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
