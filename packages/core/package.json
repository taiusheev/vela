{
  "name": "@vela/core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure domain logic: local time, the exchange state machine, the daily schedule, ask selection, arrival rendering, button codec, idempotency keys, tuning, parent commands, turns, read-back. No I/O.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@vela/contracts": "workspace:*",
    "@vela/copy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
